<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.podstone">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.ChannelActivity" />
        <activity android:name=".ui.ShowActivity" />

        <provider
            android:name=".data.provider.ShowContentProvider"
            android:authorities="com.example.android.podstone"
            android:exported="false" />

        <activity android:name=".ui.SettingsActivity" />
        <activity android:name=".ui.FavoritesActivity" />
        <activity android:name=".ui.AboutActivity"></activity>

        <!--<receiver android:name=".ui.ShowActivity$MediaReceiver">-->
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MEDIA_BUTTON" />-->
            <!--</intent-filter>-->
        <!--</receiver>-->
        <service android:name="com.example.android.playerservicelib.service.MediaPlaybackService" android:exported="true"/>
    </application>

</manifest>